import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
      resume web version
      <div>
        download the resume
      </div>
    </div>
  )
}

export default page
